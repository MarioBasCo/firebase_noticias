import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stip.ionic.nitiecuador',
  appName: 'NotiEcuador',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
